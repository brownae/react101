console.log('Utils.js is running!');

const square = (x) => x * x;
export const add = (a,b) => a + b;

export { square }; // this is for passing in various things we want to export.
// exports - 1. default export 2. named exports
